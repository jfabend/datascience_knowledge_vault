The calculation [[z-score]] or Standard Scaling is the transformation of the value of a random variable X into its distance to the mean in relationship to the standard deviation:

z = (X - mean) / std